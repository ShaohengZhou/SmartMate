import { AmplifyService, AuthState } from '../../../providers';
export declare class AuthenticatorComponentCore {
    authState: AuthState;
    amplifyService: AmplifyService;
    constructor(amplifyService: AmplifyService);
    hide: string[];
    subscribe(): void;
    shouldHide(comp: any): boolean;
}
