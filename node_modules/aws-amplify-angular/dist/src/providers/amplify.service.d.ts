import { Subject } from 'rxjs';
import { Observable } from 'rxjs';
import { AuthClass, AnalyticsClass, StorageClass, APIClass, InteractionsClass } from 'aws-amplify';
import { AuthState } from './auth.state';
export declare class AmplifyService {
    private _auth;
    private _analytics;
    private _storage;
    private _api;
    private _cache;
    private _pubsub;
    private _interactions;
    private _authState;
    authStateChange$: Observable<AuthState>;
    constructor();
    auth(): AuthClass;
    analytics(): AnalyticsClass;
    storage(): StorageClass;
    api(): APIClass;
    interactions(): InteractionsClass;
    cache(): any;
    pubsub(): any;
    authState(): Subject<AuthState>;
    setAuthState(state: AuthState): void;
}
