import { Location } from '@angular/common';
import { NavigationExtras, Router, UrlTree } from '@angular/router';
export declare const enum NavIntent {
    Auto = 0,
    Forward = 1,
    Back = 2,
    Root = 3
}
export declare class NavController {
    private location;
    private router?;
    private intent;
    private animated;
    private stack;
    constructor(location: Location, router?: Router | undefined);
    navigateForward(url: string | UrlTree | any[], animated?: boolean, extras?: NavigationExtras): Promise<boolean>;
    navigateBack(url: string | UrlTree | any[], animated?: boolean, extras?: NavigationExtras): Promise<boolean>;
    navigateRoot(url: string | UrlTree | any[], animated?: boolean, extras?: NavigationExtras): Promise<boolean>;
    goBack(animated?: boolean): void;
    setIntent(intent: NavIntent, animated?: boolean): void;
    consumeTransition(): {
        direction: number;
        animated: boolean;
    };
    private guessDirection;
}
